export enum DashboardState {
  DASHBOARD = 'dashboard',
  MISSION = 'mission',
  FACTURES = 'factures',
  PROFIL = 'profil',
  DOCUMENTS = 'documents',
}

export enum AdminDashboardState {
  DASHBOARD = 'dashboard',
  USERS = 'users',
  MISSIONS = 'missions',
  DOCUMENTS = 'documents'
}
