export enum DashboardState {
  DASHBOARD = 'dashboard',
  MISSION = 'mission',
  FACTURES = 'factures',
  PROFIL = 'profil',
  DOCUMENTS = 'documents',
  STATISTICS = 'statistics',
  GROUP = 'group',
}

export enum AdminDashboardState {
  DASHBOARD = 'dashboard',
  USERS = 'users',
  MISSIONS = 'missions',
  DOCUMENTS = 'documents'
}

export enum ModalType {
  REFUS = 'refus',
  ACCEPT = 'accept'
}
